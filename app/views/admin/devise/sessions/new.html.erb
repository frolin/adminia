<h2>Log in</h2>


<div class="loginColumns animated fadeInDown">
  <div class="row">

    <div class="col-md-6">
      <h2 class="font-bold">Welcome to IN+</h2>

      <p>
        Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
      </p>

      <p>
        Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's
        standard dummy text ever since the 1500s.
      </p>

      <p>
        When an unknown printer took a galley of type and scrambled it to make a type specimen book.
      </p>

      <p>
        <small>It has survived not only five centuries, but also the leap into electronic typesetting, remaining
          essentially unchanged.</small>
      </p>C

    </div>
    <div class="col-md-6">
      <div class="ibox-content">


        <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: 'm-t') do |f| %>

          <div class="form-group">
            <%= f.email_field :email, class: 'form-control', autofocus: true, autocomplete: "email", placeholder: I18n.t('activerecord.attributes.user.email') %>
          </div>
          <div class="form-group">
            <%= f.password_field :password, class: 'form-control', autocomplete: "current-password", placeholder: I18n.t('activerecord.attributes.user.password') %>
          </div>

          <div class="actions">
            <%= f.submit I18n.t('devise.sessions.new.sign_in'), class: 'btn btn-primary block full-width m-b' %>
          </div>


          <% if devise_mapping.rememberable? %>
            <div class="form-group">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>

          <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
            <%= link_to I18n.t('devise.shared.links.forgot_your_password'), new_password_path(resource_name) %><br/>
          <% end %>

        <% end %>

        <p class="m-t">
          <small></small>
        </p>
      </div>
    </div>
  </div>
  <hr/>
  <div class="row">
    <div class="col-md-6">
      <%= I18n.t('.login.info') %>
    </div>
    <div class="col-md-6 text-right">
      <small>© <%= "#{Time.now.year}" %></small>
    </div>
  </div>
</div>



<%#= render "devise/shared/links" %>
